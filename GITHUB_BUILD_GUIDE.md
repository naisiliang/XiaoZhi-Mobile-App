# GitHub Actions 构建 v0.5.0 APK

1. 解压本工程。
2. 双击 `PUSH_TO_GITHUB_MANUAL_PROXY.bat`（如果需要代理，输入已验证的 HTTP/Mixed Port）。
3. 推送成功后打开仓库 Actions。
4. 进入最新 `Build XiaoZhi Mobile APK`。
5. 等待 `Build debug APK` 与 `Upload artifact` 变绿。
6. 下载 `XiaoZhi-Mobile-APK` Artifact。
7. 解压得到 `XiaoZhi-Mobile-v0.5.0-debug.apk`。
