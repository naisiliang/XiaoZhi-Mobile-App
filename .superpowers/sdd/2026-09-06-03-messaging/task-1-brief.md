# Messaging Task 1 brief

Implement only the immutable messaging state and confirmation-token model from
the approved Phase 2 plan. The token must expire at exactly 60,000 ms and bind
the app/package, exact contact identity/display name, conversation title,
message body, ScreenContext generation, and window fingerprint. Any mismatch
is invalid; no send or UI execution belongs in this task.
